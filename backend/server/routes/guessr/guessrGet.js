const express = require("express");
const router = express.Router();
const newUserModel = require('../models/userModel')

router.post('/guessrGet', async (req, res) => {

    

  })

  module.exports = router;